<div class="card">
    <div
        style="display: flex; justify-content: space-between; align-items: center; border-bottom: 1px solid #0f0; padding-bottom: 1rem; margin-bottom: 1rem;">
        <h2>SECURE VAULT</h2>
        <button class="btn" onclick="showManual('Cryptographic Failures', `<%= manual %>`)">MISSION BRIEFING</button>
    </div>

    <p>Welcome to the SOVAP Secure Vault. All notes are encrypted using our proprietary military-grade algorithm.</p>

    <div style="margin-top: 2rem;">
        <h3>ENCRYPTED STORAGE</h3>
        <table style="width: 100%; border-collapse: collapse; margin-top: 1rem;">
            <thead>
                <tr style="border-bottom: 1px solid #0f0;">
                    <th style="padding: 0.5rem; text-align: left;">ID</th>
                    <th style="padding: 0.5rem; text-align: left;">OWNER</th>
                    <th style="padding: 0.5rem; text-align: left;">CIPHERTEXT (Base64)</th>
                </tr>
            </thead>
            <tbody>
                <% notes.forEach(note=> { %>
                    <tr style="border-bottom: 1px solid #333;">
                        <td style="padding: 0.5rem;">
                            <%= note.id %>
                        </td>
                        <td style="padding: 0.5rem;">
                            <%= note.owner %>
                        </td>
                        <td style="padding: 0.5rem; font-family: monospace; color: orange;">
                            <%= note.content %>
                        </td>
                    </tr>
                    <% }) %>
            </tbody>
        </table>
    </div>

    <div style="margin-top: 2rem; border-top: 1px solid #0f0; padding-top: 1rem;">
        <h4>LEAKED SOURCE CODE SNIPPET</h4>
        <pre style="background: #111; padding: 1rem; border: 1px solid #333; overflow-x: auto;">
function encrypt(text) {
    let key = 42; // Chosen by fair dice roll
    let result = '';
    for (let i = 0; i < text.length; i++) {
        result += String.fromCharCode(text.charCodeAt(i) ^ key);
    }
    return Base64.encode(result);
}
        </pre>
    </div>
</div>